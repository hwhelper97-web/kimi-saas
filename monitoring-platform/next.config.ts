import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
  /* PWA features and environment routing variables can be injected here */
};

export default nextConfig;
